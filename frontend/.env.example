# API URL - Backend server location
# IMPORTANT: This MUST be set for the frontend to work correctly
# Without this, API calls will fail or timeout, causing extreme slowness
NEXT_PUBLIC_API_URL=http://localhost:3001
